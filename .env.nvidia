# NVIDIA GPU Configuration
GPU_TYPE=nvidia
DOCKER_RUNTIME=nvidia

# NVIDIA-specific settings
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=all

# Empty for NVIDIA (handled by runtime/driver)
GPU_DEVICES=
GPU_GROUPS=
SECURITY_OPTS=

# Deploy section specifically for NVIDIA
DEPLOY_DEVICES=[{"driver":"nvidia","count":"all","capabilities":["gpu"]}]

# VNC Configuration
VNC_PASSWORD=antigravity
VNC_PORT=5901
NOVNC_PORT=6080

# Display Configuration
DISPLAY_WIDTH=1920
DISPLAY_HEIGHT=1080

# Application Settings
AUTOSTART_ANTIGRAVITY=true
IDLE_TIMEOUT=60
